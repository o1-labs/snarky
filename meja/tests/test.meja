let x = 15i;

let y = x;

let z = (x);

let a = (15i);

let b = {let y = x; y;};

let c = fun (ignore : int -> unit) => {
  ignore(x);
  y;
};

let d = fun (x) => { x; };

let e = {let e = {let e = x; e;}; e;};

let f = {
  let f = fun (x) => { x; };
  let g = d (f, f);
  g (f, f) (f);
};

let g : _ -> _ = {
  let f : _ -> _ = fun (x) => { x; };
  f;
};

let h = fun (ignore : int -> unit) => {
  let a = 1i;
  let b = 2i;
  ignore (a);
  ignore (b);
  let c = 3i;
  let d = 4i;
  ignore (c);
  ignore (d);
  let e = 5i;
  ignore (e);
  let f = fun (x, y) => {x;};
  f (a, f (b, f (c, f (d, e))));
};

let i = fun () => { (); };

let j = i ();
