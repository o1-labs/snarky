let x = 15;

let y = x;

let z = (x);

let a = (15);

let b = (let y = x; y);

let c = fun (ignore : int -> unit) => {
  ignore(x);
  y;
};

let d = fun (x) => { x; };

let e = let e = let e = x; e; e;

let f =
  let f = fun (x) => { x; };
  let g = d (f, f);
  g (f, f) (f);

let g : _ -> _ =
  let f : _ -> _ = fun (x) => { x; };
  f;
