module A = {
  let x = 15;
  let y = 20;

  Prover {
    let x = true;
    let y = false;
  };
};

let z = A.x + A.y;

Prover {
  let z = A.x && A.y;
};

let z : opaque(int) = {
  let x = A.x;
  let y = A.y;
  Prover {
    if (A.x && A.y) { x; } else { y; };
  };
};
