module A = {
  let x = 15;
  let y = 20;

  Prover {
    let a = true;
    let b = false;
  };
};

let z = A.x + A.y;

Prover {
  let z = A.a && A.b;
};

let z : int = if (Prover {A.a && A.b;}) { A.x; } else { A.y; };
