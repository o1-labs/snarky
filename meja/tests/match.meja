let x = fun (y : int, z : bool) => {
  switch (y) {
    | i => z
  };
};

let y = fun (f : int -> int -> int -> _) => {
  switch (1, 2, 3) {
    | (i, j, k) => f (i, j, k)
  };
};

type t = {a: int, b: bool};

type u = {f : int -> int};

let z = fun (x, {f}) => {
  switch (x) {
    | {a: x, b} => f (x)
  };
};

let a = fun (x : t, {f}) => {
  switch (x) {
    | {a: x, b} => f (x)
  };
};
