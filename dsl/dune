(executable
 (name meja)
 (libraries core_kernel)
 (preprocess (pps ppx_sexp_conv))
 (flags :standard -warn-error -39-21))

(menhir
  (flags --explain)
  (modules parser))

(ocamllex
  (modules lexer))
